
  <h1>City Name: <%= @city.name %></h1>

<h4>List of citizens</h4>
<% if @city.citizens.any?%>
  <ul>
    <% @city.citizens.each do |citizen| %>
    <p> Citizen</p>
      <li class="new"><span class="tag">Name:</span> <%= citizen.name %>,
      <span class="tag">Age:</span> <%= citizen.age %>,
      <span class="tag">Occupation:</span> <%= citizen.occupation %></li>
    <% end %>
  </ul>
<% else %>
  <p>There are no citizens in this city.</p>
<% end %>

<p> <%= link_to "Delete", city_path(@city.id), method: :delete, data: { confirm: "Are you sure?"} %> </p>
